/** Route group: URLs stay `/vendor`, `/...` — segment name does not appear in path. */
export default function DashboardGroupLayout({ children }: { children: React.ReactNode }) {
  return children;
}
